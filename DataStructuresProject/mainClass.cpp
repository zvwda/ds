#include<iostream>
using namespace std; 
/*#include<vector>
#include<set>
#include "Course.h"
#include "admin.h"
#include "files_mg.h"
#include "GlobalDataa.h"    
#include "Student.h"*/
void main() {


    cout << " djvn"; 
  /*  Graph graph;

    // Adding courses
    Course ds("Data Structures", 101, true, 50, 3.0, "John Doe");
    Course as("Algorithms and Data Structures", 102, true, 50, 3.0, "Jane Smith");
    Course fr("Programming Fundamentals", 103, true, 50, 3.0, "John Doe");
    Course jh("Java for Beginners", 104, true, 50, 3.0, "Jane Smith");
    Course ll("Linear Algebra", 105, true, 50, 3.0, "John Doe");
    graph.add_course(ds);
    graph.add_course(as);
    graph.add_course(fr);
    graph.add_course(jh);
    graph.add_course(ll);

    // Adding prerequisites
    graph.add_prerequisite(as, "Data Structures");
    graph.add_prerequisite(as, "Linear Algebra");
    graph.add_prerequisite(ds, "Programming Fundamentals");
    graph.add_prerequisite(ds, "Calculus");
    graph.add_prerequisite(jh, "Programming Fundamentals");

    // Printing the graph
    graph.print_graph();

    // Printing prerequisites for each course
    ds.print_prerequisites();
    as.print_prerequisites();
    ll.print_prerequisites();
    jh.print_prerequisites();*/
}